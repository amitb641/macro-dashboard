__pycache__/
*.pyc
*.pyo
.env
.DS_Store
mockups/*.preview.png

# Scratch dir created by tests/test_validator_passes.py (broken-fixture
# tmp files kept inside the repo so validator's Path.relative_to() calls
# in error messages remain valid). Cleaned up in test teardown.
tests/_scratch/
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Derived query index for data/monthly_archive/ — rebuildable in <1s via
# scripts/build_archive_index.py, deliberately not committed (binary
# SQLite diffs bloat git history for zero benefit; the plain JSON/HTML
# files under data/monthly_archive/ are the actual source of truth).
data/monthly_archive.db
