# Repair Diagnostician — Incident Report
_Run at 2026-06-26 14:01 UTC_

- Findings diagnosed: 2
- LLM calls used: 2 / 20
- Memory stats: {"total": 2000, "by_agent": {"unknown": 932, "editorial": 1012, "explainer": 34, "repair": 22}, "errors": 1988}
- Guardrails: {"disabled": false, "diagnostician_enabled": true, "max_llm_calls": 20, "calls_used": 2, "anthropic_key_set": true}

### Finding: UMich Direct CSV Endpoint Returning 404
**Severity**: warning
**Confidence**: medium
**Recurrence**: seen 2/10 runs; prior diagnosis failed due to API credit exhaustion (no actionable prior fix)
**Root cause (hypothesis)**: The UMich collector is attempting to fetch sentiment data directly from `https://www.sca.isr.umich.edu/files/tbcics.csv`, and that URL is returning a 404. This could indicate a periodic file rotation or path change on the UMich server (the CSV filename or directory structure may change with each release cycle), or the endpoint may be intermittently unavailable. Because this has appeared in 2 of 10 runs without a clear pattern, transient server-side unavailability or a URL that changes with each publication cycle are the most likely causes.
**Playbook citation**: §2.2 (UMich publish lag: mid-month prelim, end-of-month final); §3.1 Pass 3h (collector errors — network/quota issues); baselines `publish_lag_days.umich`
**Recommended next step**:
- Manually verify the current CSV URL at `https://www.sca.isr.umich.edu` and, if the path has changed, update the endpoint in the UMich collector (the file responsible for fetching `tbcics.csv`) to match the live path.

**Rationale**: Playbook §3.1 Pass 3h classifies this as a network/source issue, not a data-content bug. The UMich baseline (§2.2, baselines `publish_lag_days.umich`) notes a typical lag of up to 14 days, so a transient 404 near a publish boundary is plausible — but a 404 (not a timeout or 5xx) more strongly suggests a URL path problem than a timing issue. The 2/10 recurrence rate is below the 3-run threshold for mandatory root-cause escalation, but the prior diagnosis was a non-diagnosis (API credit failure), so no fix has ever been proposed or validated. A human should confirm the live URL before the next run to prevent this from becoming a persistent data gap for UMich sentiment.
**Gap (if any)**: Knowing *which* runs the 404 occurred on (e.g., mid-month vs. end-of-month) would clarify whether this is tied to UMich's publish cycle (prelim vs. final release) or is truly random. Access to the collector source file (file name and line where `tbcics.csv` URL is constructed) would allow a precise file:line citation and raise confidence to high.

---

### Finding: Staleness — umcsent (UMich Sentiment) exceeds max_lag_days
**Severity**: stale
**Confidence**: high
**Recurrence**: recurring; seen 4/10 runs since prior to 2026-06-26; prior diagnoses failed due to API credit exhaustion (not a data fix)
**Root cause (hypothesis)**: The `umcsent` series is flagged at 86 days old against a pipeline `max_lag_days` of 35. However, the two prior "diagnoses" were billing failures, not actual investigations — so the underlying data condition has persisted unexamined. UMich Sentiment's normal publish lag is 14 days (typical_max) per the baselines, making 86 days genuinely anomalous: it exceeds the `investigate_if_over` threshold of 25 days by 61 days. This is not within normal source lag and warrants active investigation of the collector.
**Playbook citation**: §2.2 (UMich publish lag: prelim mid-month, final end-of-month; typical_max 14 days, investigate_if_over 25 days); baselines `publish_lag_days.umich`; §4.1 (lag exceeds table by >7 days → investigate collector fetch parameters)
**Recommended next step**:
- Inspect the UMich/FRED collector for `umcsent` (likely `UMCSENT` FRED series): verify the API fetch is returning the latest observation, check `limit=` parameter is not capping results short of the current date, and confirm the FRED endpoint has not changed; if the fetch looks correct, check whether FRED itself has a data gap for `UMCSENT` around the expected publish window (mid/end of May 2026).

**Rationale**: At 86 days, `umcsent` exceeds the `investigate_if_over` threshold of 25 days by more than 60 days — this is not a normal publish-lag situation per §2.2 and the baselines. Playbook §4.1 directs investigation of (1) source outage, (2) collector `limit=` parameter, and (3) API endpoint change when lag exceeds the table by >7 days. The recurrence across 4 of 10 runs, combined with the fact that prior "diagnoses" were billing failures rather than actual resolutions, means this issue has been silently persisting without a real fix attempt — the recurrence pattern should be treated as "investigate root cause" per recurrence escalation rules, not merely monitored.
**Gap (if any)**: The exact collector file and function name for `umcsent` would allow a precise `limit=` bump recommendation (analogous to §4.2's pattern). Confirming whether FRED's `UMCSENT` series itself shows a data gap at the relevant date would distinguish a collector bug from a source outage.
