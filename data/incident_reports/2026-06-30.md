# Repair Diagnostician — Incident Report
_Run at 2026-06-30 12:58 UTC_

- Findings diagnosed: 1
- LLM calls used: 1 / 20
- Memory stats: {"total": 2000, "by_agent": {"unknown": 909, "editorial": 1022, "explainer": 46, "repair": 23}, "errors": 1976}
- Guardrails: {"disabled": false, "diagnostician_enabled": true, "max_llm_calls": 20, "calls_used": 1, "anthropic_key_set": true}

### Finding: cs_hpi staleness flag — age 121 days vs limit 120 days
**Severity**: stale
**Confidence**: low
**Recurrence**: first observed (1/10 runs, last seen 2026-06-30)
**Root cause (hypothesis)**: The Case-Shiller HPI series (`cs_hpi`) is flagged at 121 days old against a 120-day `max_lag_days` limit — a margin of just 1 day. No baseline entry for `cs_hpi` exists in the known-normal baselines or playbook §2.2 publish-lag table, so the configured limit of 120 days cannot be verified against a curated typical-lag figure. The most likely explanation is that the source has not yet published its latest release and the pipeline ran 1 day before the data arrived, making this a borderline timing artifact rather than a genuine data gap.
**Playbook citation**: §2.2 (publish lag table — `cs_hpi` not listed); §4.1 (staleness within typical range → monitor, not fix); baselines `publish_lag_days` block (no `cs_hpi` entry)
**Recommended next step**:
- Monitor only — check whether `cs_hpi` has published by the next weekly run (≈ 2026-07-07); if still absent, escalate to investigate source cadence and whether `max_lag_days=120` in the collector is correctly calibrated for Case-Shiller's actual release schedule.

**Rationale**: The overage is 1 day (121 vs 120), which is well within any reasonable timing jitter for a monthly or quarterly housing index. Playbook §4.1 instructs that a staleness flag within the source's typical publish lag is informational, not actionable — and since `cs_hpi` is absent from the §2.2 table and the baselines JSON, the 120-day limit itself is unverified. This is a first-time observation across 10 runs, consistent with a one-off scheduling coincidence rather than a structural problem; playbook guidance is to avoid over-reacting to a single noisy run.

**Gap (if any)**: `cs_hpi` has no entry in the `publish_lag_days` baselines block and is not listed in playbook §2.2. Adding a curated `typical_max` and `investigate_if_over` value for Case-Shiller HPI (S&P CoreLogic, typically released ~60 days after reference month) would allow the Diagnostician to assess future flags with high confidence rather than low. A human reviewer should confirm the correct release cadence and update the baselines file accordingly.
