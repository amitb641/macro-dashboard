# Repair Diagnostician — Incident Report
_Run at 2026-06-30 13:21 UTC_

- Findings diagnosed: 1
- LLM calls used: 1 / 20
- Memory stats: {"total": 2000, "by_agent": {"editorial": 1034, "unknown": 890, "explainer": 52, "repair": 24}, "errors": 1968}
- Guardrails: {"disabled": false, "diagnostician_enabled": true, "max_llm_calls": 20, "calls_used": 1, "anthropic_key_set": true}

### Finding: Staleness flag for cs_hpi (age 121 days, limit 120 days)
**Severity**: stale
**Confidence**: low
**Recurrence**: seen 2/10 runs; not yet recurring threshold
**Root cause (hypothesis)**: The Case-Shiller HPI (cs_hpi) series is flagged at 121 days old against a 120-day `max_lag_days` limit — a margin of just 1 day. The playbook's §2.2 publish lag table does not include Case-Shiller HPI as a named source, so no authoritative typical-lag baseline exists to confirm whether this is within normal publish behavior. Case-Shiller is a monthly index released with a roughly 60–90 day lag by S&P/CoreLogic, but the pipeline's configured limit of 120 days suggests the maintainers already built in generous headroom; a 1-day overage is most likely a borderline timing artifact rather than a genuine data gap.
**Playbook citation**: §2.2 (publish lag table) — cs_hpi is not listed; §4.1 (staleness pattern guidance); baselines JSON `publish_lag_days` — cs_hpi has no entry.
**Recommended next step**:
- Monitor only through the next weekly run (Saturday 12:00 UTC); if cs_hpi remains flagged for 3+ consecutive runs, investigate whether the collector's `max_lag_days` limit for cs_hpi should be raised to 130 days or whether the S&P/CoreLogic API endpoint has changed.

**Rationale**: At just 1 day over the configured limit, this is almost certainly a publish-timing edge case rather than a collector failure — playbook §4.1 instructs that staleness within or near the source's normal lag window should not be treated as actionable. The recurrence rate (2/10 runs) is below the 3-run threshold that would trigger a root-cause investigation per the recurrence rules, and the two occurrences are not consecutive, suggesting intermittent timing sensitivity around the publish window rather than a persistent failure. This is a first-pattern finding in the sense that no prior diagnosis has proposed a fix, so there is no evidence of an unapplied correction.

**Gap (if any)**: The playbook §2.2 table and the baselines JSON do not include Case-Shiller HPI (`cs_hpi`) as a named source with a documented typical lag. Adding a `cs_hpi` entry to the `publish_lag_days` baseline (with S&P/CoreLogic's documented release schedule) would allow the Diagnostician to make a high-confidence determination of whether 121 days is within normal range, and would clarify whether the 120-day `max_lag_days` in the collector is correctly calibrated.
